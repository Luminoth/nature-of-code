[package]
name = "ecosystem"
version = "0.1.0"
authors = ["Shane Lillie <ignignokterr@gmail.com>"]
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
bevy = "0.5"
bevy_rapier2d = { version = "0.9", features = [ "simd-stable", "parallel", "serde-serialize" ] }
derivative = "2.2"
rand = "0.8"
